<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="vitoria.apsapp">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.INTERNET"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Index">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TecidoAnimal" />
        <activity android:name=".AdicionaQuestao" />
        <activity android:name=".TecidoVegetal" />
        <activity android:name=".Questionario1" />
        <activity android:name=".ListaDeQuestoes" />
        <activity android:name=".Login" />
        <activity android:name=".IndexAdministrador" />
        <activity android:name=".Camera" />
        <activity android:name=".ListaDispositivos"
            android:theme="@android:style/Theme.Dialog"
            android:label="Dispositivos Pareados"/>
        <activity android:name=".EliminaQuestao"></activity>
    </application>

</manifest>